[project]
name = "fastapi-pep621-fixture"
version = "0.1.0"
dependencies = [
  "fastapi>=0.110.0",
  "sqlalchemy>=2.0.29",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
